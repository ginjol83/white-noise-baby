import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'white.noise.baby',
  appName: 'white-noise-baby',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
